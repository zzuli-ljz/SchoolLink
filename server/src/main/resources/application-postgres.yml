spring:
  datasource:
    url: jdbc:postgresql://ep-plain-boat-ahdylonx-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require&applicationName=schoollink-server
    username: ${POSTGRES_USER:neondb_owner}
    password: ${POSTGRES_PASSWORD:npg_CDTnPtp4B5xm}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081